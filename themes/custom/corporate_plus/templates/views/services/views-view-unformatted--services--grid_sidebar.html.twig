<section class="services-2-grid">
    <div class="services-style-1">
        <div class="container">
            <div class="row s-15">
                {% for row in rows %}
                    {{row.content}}
                {% endfor %}
            </div>
        </div>
    </div>
</section>