{#
/**
 * @file
 * Template for the logo image field.
 */
#}

<!-- DEBUG: TEMPLATE CAMPO CARGADO -->
<div style="background: blue; color: white; padding: 10px; margin: 10px; font-family: monospace; position: fixed; top: 400px; left: 20px; z-index: 9999; border: 3px solid black;">
  <strong>DEBUG TEMPLATE CAMPO:</strong><br>
  Template campo cargado: SÍ<br>
  Items count: {{ items|length }}<br>
  {% for item in items %}
    Item content exists: {{ item.content is defined ? 'YES' : 'NO' }}<br>
  {% endfor %}
</div>

{% for item in items %}
  {% if item.content %}
    {# Solo mostrar la imagen, sin etiquetas ni texto #}
    <img src="{{ file_url(item.content['#item'].entity.uri.value) }}"
         alt="{{ item.content['#item'].alt }}"
         style="height: 80px; width: auto; object-fit: contain;" />
  {% endif %}
{% endfor %} 